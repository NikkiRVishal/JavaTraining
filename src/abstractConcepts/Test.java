package abstractConcepts;

public abstract class Test {

    // If a class has method declaration then that class must be abstract class
    //If a class is an abstract class and don't have any abstract method then it's called 0% abstraction
   // If a class is an abstract class and have only abstract method then it's called 100% abstraction
    // If a class is an abstract class and have abstract and non abstract methods then it's called partial abstraction
    // Interface is 100% abstraction
   abstract void test();

   void test(int a){

   }
}
